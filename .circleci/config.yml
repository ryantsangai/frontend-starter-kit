# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2

defaults: &defaults
  working_directory: ~/repo
  docker:
    - image: circleci/node:6-browsers

jobs:
  install:
    <<: *defaults
    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "package.json" }}
          # fallback to using the latest cache if no exact match is found
          - v1-dependencies-

      - run: npm install

      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}

  build:
    <<: *defaults
    steps:
      - client: npm run build:client
      - server: npm run build:server
      - prerender: npm run build:prerender
      - functions: npm run build:functions
      - storybook: npm run build:storybook

  deploy:
    <<: *defaults
    steps:
      - firebase: firebase deploy --token=$FIREBASE_TOKEN --non-interactive
